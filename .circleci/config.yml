# Golang CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-go/ for more details
version: 2
jobs:
  build:
    machine: true
    # docker:
    #   # specify the version
    #   - image: circleci/golang:1.11

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4

    #### TEMPLATE_NOTE: go expects specific checkout path representing url
    #### expecting it in the form of
    ####   /go/src/github.com/circleci/go-tool
    ####   /go/src/bitbucket.org/circleci/go-tool
    working_directory: $HOME/go/src/github.com/src-d/gitbase
    steps:
      #- setup_remote_docker
      - checkout
      - run: &install
          name: Install dependancies 
          command: |
            sudo apt-get update
            sudo apt-get install build-essential software-properties-common -y
            sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
            sudo apt-get update
            sudo apt-get install -y gcc g++-6 wget
            sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-6 90
            sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-6 90
            wget http://mirrors.kernel.org/ubuntu/pool/main/m/make-dfsg/make_4.1-9.1ubuntu1_amd64.deb
            dpkg -i make_4.1-9.1ubuntu1_amd64.deb
            docker run -d --name bblfshd --privileged -p 9432:9432 -v /var/lib/bblfshd:/var/lib/bblfshd bblfsh/bblfshd
            docker exec -it bblfshd bblfshctl driver install python bblfsh/python-driver
            docker exec -it bblfshd bblfshctl driver install php bblfsh/php-driver
            docker exec -it bblfshd bblfshctl driver install go bblfsh/go-driver


      # specify any bash command here prefixed with `run: `
      - run: make test-coverage codecov
